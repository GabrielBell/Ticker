import React from 'react';
import * as Redux from 'react-redux';
var actions = require('actions');

export var AddTicker = React.createClass({
	render: function(){
		return();
	}
});

export default Redux.connect()(AddTickerFilteredList);